logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
server:
  error:
    include-message: always
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/me_leva_ai
    username: postgres
    password: joao.schein
  jpa:
    hibernate:
      ddl-auto: create-drop
